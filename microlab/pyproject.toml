[project]
name = "microlab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9.10"
dependencies = [
    "jupyter>=1.1.1",
    "pyspark>=4.0.2",
]
