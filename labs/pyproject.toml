[project]
name = "labs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.10"
dependencies = [
    "faker>=40.5.1",
    "tqdm>=4.67.3",
]

[tool.uv.workspace]
members = [
    "lab05",
]
